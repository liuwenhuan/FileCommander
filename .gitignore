# Build directories
build/
build-deb/
build-appimage/

# In-source CMake build artifacts (a stray `cmake .` at the repo root)
/CMakeCache.txt
CMakeFiles/
cmake_install.cmake
CTestTestfile.cmake
Makefile
DartConfiguration.tcl
compile_commands.json
_deps/

# Qt meta-object / resource / uic output
*_autogen/

# FetchContent'd GoogleTest
/googletest-src/
/googletest-build/

# Compiled objects / archives / the built binary
*.o
*.a
/ttc

# Compiled test / benchmark executables. These were tracked by mistake before
# this rule existed (~36 MB of ELF in the history); untracked as of the
# packaging work, but the files stay on disk for whoever is mid-run.
/tests/core/core_tests
/tests/ui/ui_tests
/tests/archive/archive_tests
/tests/viewer/viewer_tests
/bench/bench_dirlisting

# Packaging output and downloaded tooling
/dist/
/AppDir/
packaging/.tools/
*.deb
*.AppImage

# Editor / tooling
.claude/

# CTest output
Testing/
