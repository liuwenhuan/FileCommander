# Archive browsing/extraction lands in Phase 2 of the roadmap
# (docs/... total-commander-design.md). Placeholder target so the root
# CMakeLists' target_link_libraries(ttc ... archive ...) resolves today.
add_library(archive INTERFACE)
